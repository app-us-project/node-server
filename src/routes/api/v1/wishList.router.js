const express = require('express');
const router = express.Router();
//const {addWishList, getAllWishList, deleteWishList} = require('../../../controllers/wishList.controller');

//router.post('/:productId/wishList', addWishList);
//router.get('/wishList/', getAllWishList);
//router.delete('/wishList/:wishListId', deleteWishList);

module.exports  = router;